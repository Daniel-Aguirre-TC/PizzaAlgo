1. Decide which business to order from
2. Decide which type of pizza to order
3. Select Marcos If Exists

if (WrongOrder())
{
    // Code for if we received the wrong order.
}

// This text is only in my test branch. I created this test branch by typing the following line in GitBash:
// git checkout -b test